//			right rotation testing code
if(node.getValue() == 9)
			{
				rightRotate(node.getParent());
				return true;
			}
      
      
//			left rotation testing code
			if(node.getValue() == 3)
			{
				leftRotate(node.getParent());
				return true;
			}
